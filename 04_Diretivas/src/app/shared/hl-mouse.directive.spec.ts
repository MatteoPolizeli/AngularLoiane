import { HlMouseDirective } from './hl-mouse.directive';

describe('HlMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HlMouseDirective();
    expect(directive).toBeTruthy();
  });
});
